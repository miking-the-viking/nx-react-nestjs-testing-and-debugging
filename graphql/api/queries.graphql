fragment coreApiUserFields on User {
    id
    created_at
    updated_at
    email
    role
}

query Me {
    me {
        id
        email
        role
        created_at
        updated_at
    }
}
